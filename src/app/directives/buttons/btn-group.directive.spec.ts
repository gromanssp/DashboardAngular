import { BtnGroupDirective } from './btn-group.directive';

describe('BtnGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnGroupDirective();
    expect(directive).toBeTruthy();
  });
});
