import { DGroupToolbarDirective } from './d-group-toolbar.directive';

describe('DGroupToolbarDirective', () => {
  it('should create an instance', () => {
    const directive = new DGroupToolbarDirective();
    expect(directive).toBeTruthy();
  });
});
