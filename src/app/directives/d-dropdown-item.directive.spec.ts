import { DDropdownItemDirective } from './d-dropdown-item.directive';

describe('DDropdownItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DDropdownItemDirective();
    expect(directive).toBeTruthy();
  });
});
