import { DButtonDirective } from './d-button.directive';

describe('DButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DButtonDirective();
    expect(directive).toBeTruthy();
  });
});
